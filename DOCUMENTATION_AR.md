# 🚑 نظام ResQ - توثيق شامل للمشروع

## 📋 جدول المحتويات
1. [نظرة عامة على المشروع](#نظرة-عامة)
2. [البنية التقنية](#البنية-التقنية)
3. [هيكل المشروع](#هيكل-المشروع)
4. [شرح الملفات الرئيسية](#شرح-الملفات)
5. [دليل الاستخدام](#دليل-الاستخدام)
6. [قاعدة البيانات](#قاعدة-البيانات)
7. [المكونات (Components)](#المكونات)

---

## 🎯 نظرة عامة على المشروع {#نظرة-عامة}

### الهدف
نظام **ResQ** هو تطبيق ويب لإدارة وتنسيق عمليات إرسال سيارات الإسعاف في حالات الطوارئ. يهدف إلى:
- تسريع استجابة خدمات الطوارئ
- تنظيم عمليات توزيع سيارات الإسعاف
- تتبع الحوادث والبلاغات
- إدارة أسطول المركبات والطواقم الطبية

### المستخدمون المستهدفون
1. **المنظّم (Dispatcher)**: يستقبل البلاغات ويوزع سيارات الإسعاف
2. **مدير الأسطول**: يدير حالة المركبات والصيانة
3. **المشرف**: يراقب الأداء والإحصائيات

---

## 🏗️ البنية التقنية {#البنية-التقنية}

### التقنيات المستخدمة

#### الواجهة الأمامية (Frontend)
```
⚛️ React 19        - مكتبة بناء واجهات المستخدم
🎨 CSS Vanilla     - تنسيق الصفحات (بدون إطار عمل)
🔄 Vite           - أداة البناء والتطوير السريع
```

#### إدارة البيانات
```
📊 useState       - إدارة الحالة المحلية للمكونات
🔄 useEffect      - التعامل مع العمليات الجانبية والتحميل
🌐 Fetch API      - جلب البيانات من الخادم
```

#### الخادم الوهمي (Backend Mock)
```
🗄️ JSON Server    - خادم REST API وهمي للتطوير
📁 db.json        - قاعدة بيانات JSON بسيطة
```

### لماذا هذه التقنيات؟
- **البساطة**: لا حاجة لـ Redux أو إدارة حالة معقدة
- **الأداء**: Vite يوفر تحميلاً سريعاً جداً
- **سهولة التعلم**: استخدام React Hooks الأساسية فقط
- **التطوير السريع**: JSON Server يتيح إنشاء API كاملة بدون برمجة خلفية

---

## 📁 هيكل المشروع {#هيكل-المشروع}

```
ambilanceeCopie/
│
├── public/                     # الملفات الثابتة
│
├── src/                        # الكود المصدري
│   │
│   ├── main.jsx               # نقطة الدخول الرئيسية
│   ├── App.jsx                # المكون الرئيسي للتطبيق
│   ├── App.css                # الأنماط الرئيسية
│   ├── index.css              # الأنماط الأساسية
│   │
│   ├── services/              # طبقة التواصل مع API
│   │   └── api.js            # جميع وظائف fetch
│   │
│   ├── pages/                 # صفحات التطبيق
│   │   ├── Incidents.jsx     # صفحة إدارة الحوادث
│   │   └── Incidents.css     # أنماط صفحة الحوادث
│   │
│   ├── components/            # المكونات القابلة لإعادة الاستخدام
│   │   │
│   │   ├── layout/           # مكونات التخطيط العام
│   │   │   ├── Header.jsx   # الترويسة العلوية
│   │   │   └── Sidebar.jsx  # القائمة الجانبية
│   │   │
│   │   ├── dashboard/        # مكونات لوحة التحكم
│   │   │   ├── KPIGrid.jsx          # شبكة المؤشرات
│   │   │   ├── KPICard.jsx          # بطاقة مؤشر واحد
│   │   │   ├── IncidentList.jsx     # قائمة الحوادث
│   │   │   ├── IncidentItem.jsx     # عنصر حادث واحد
│   │   │   ├── ActivityFeed.jsx     # تدفق النشاطات
│   │   │   ├── ActivityItem.jsx     # نشاط واحد
│   │   │   ├── AmbulanceTable.jsx   # جدول سيارات الإسعاف
│   │   │   ├── AlertBanner.jsx      # لافتة التنبيهات
│   │   │   └── QuickActions.jsx     # أزرار سريعة
│   │   │
│   │   └── ui/               # مكونات واجهة عامة
│   │       ├── Modal.jsx    # نافذة منبثقة
│   │       └── StatusBadge.jsx  # شارة الحالة
│   │
│   └── data/                 # بيانات وهمية للعرض
│       ├── mockData.js
│       └── mockDataNew.js
│
├── db.json                   # قاعدة البيانات الوهمية
├── package.json             # تبعيات ونصوص المشروع
├── vite.config.js          # إعدادات Vite
├── index.html              # ملف HTML الرئيسي
│
└── README_SIMPLE.md        # دليل سريع
```

---

## 📄 شرح الملفات الرئيسية {#شرح-الملفات}

### 1️⃣ `src/main.jsx` - نقطة البداية

```jsx
// يقوم بتحميل React وتركيب التطبيق في DOM
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

**الوظيفة**: 
- نقطة الدخول الأولى للتطبيق
- يربط React بعنصر HTML (root)
- يُحمّل المكون الرئيسي App

---

### 2️⃣ `src/App.jsx` - المكون الرئيسي

#### الحالة (State)
```jsx
const [currentPage, setCurrentPage] = useState('dashboard');
const [ambulances, setAmbulances] = useState([]);
const [incidents, setIncidents] = useState([]);
const [activities, setActivities] = useState([]);
const [kpis, setKpis] = useState(null);
const [loading, setLoading] = useState(true);
const [isModalOpen, setIsModalOpen] = useState(false);
```

#### تحميل البيانات عند البدء
```jsx
useEffect(() => {
  loadData();
  
  // تحديث تلقائي كل 30 ثانية
  const interval = setInterval(loadData, 30000);
  return () => clearInterval(interval);
}, []);
```

#### وظائف التعامل مع البيانات
- `loadData()`: جلب جميع البيانات من الخادم
- `handleCreateIncident()`: إنشاء حادث جديد
- `handleAssignAmbulance()`: تعيين سيارة إسعاف لحادث
- `handleUpdateAmbulanceStatus()`: تحديث حالة السيارة

---

### 3️⃣ `src/services/api.js` - طبقة API

#### بنية الملف
```javascript
const API_URL = 'http://localhost:5000';

// ===== سيارات الإسعاف =====
export const fetchAmbulances = async () => {
  const response = await fetch(`${API_URL}/ambulances`);
  return response.json();
};

export const updateAmbulanceStatus = async (id, status) => {
  const response = await fetch(`${API_URL}/ambulances/${id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ status })
  });
  return response.json();
};
```

**الوظائف المتاحة**:
1. `fetchAmbulances()` - جلب جميع السيارات
2. `updateAmbulanceStatus()` - تحديث حالة سيارة
3. `fetchIncidents()` - جلب الحوادث
4. `createIncident()` - إنشاء حادث جديد
5. `assignAmbulance()` - تعيين سيارة لحادث
6. `fetchActivities()` - جلب سجل النشاطات
7. `fetchKPIs()` - جلب المؤشرات الإحصائية

---

### 4️⃣ `db.json` - قاعدة البيانات

#### الهيكل العام
```json
{
  "ambulances": [...],    // سيارات الإسعاف
  "incidents": [...],     // الحوادث
  "activities": [...],    // سجل الأنشطة
  "kpis": {...}          // المؤشرات
}
```

#### مثال على بيانات سيارة إسعاف
```json
{
  "id": "AMB-001",
  "callSign": "SAMU-75-01",      // الاسم الإذاعي
  "status": "available",          // الحالة
  "latitude": 48.8566,           // خط العرض
  "longitude": 2.3522,           // خط الطول
  "lastUpdate": "2025-12-14T10:30:00Z",
  "crew": {
    "driver": "Hassan Alami",
    "paramedic": "Fatima Benali"
  },
  "equipment": {
    "defibrillator": true,
    "oxygen": true,
    "stretcher": true
  }
}
```

#### مثال على بيانات حادث
```json
{
  "id": "INC-001",
  "address": "15 Avenue des Champs-Élysées, Paris",
  "latitude": 48.8698,
  "longitude": 2.3078,
  "severity": "critical",         // critical/medium/low
  "status": "in_progress",        // pending/in_progress/completed
  "patientInfo": {
    "age": 65,
    "gender": "M",
    "condition": "ألم في الصدر"
  },
  "createdAt": "2025-12-14T10:15:00Z",
  "assignedAmbulanceId": "AMB-002"
}
```

---

## 🎮 دليل الاستخدام {#دليل-الاستخدام}

### تشغيل المشروع

#### 1. تثبيت المكتبات
```bash
npm install
```

#### 2. تشغيل الخادم والواجهة معاً
```bash
npm run dev:all
```

أو بشكل منفصل:
```bash
# في terminal 1
npm run dev

# في terminal 2
npm run server
```

#### 3. فتح التطبيق
- الواجهة: `http://localhost:5173`
- API: `http://localhost:5000`

---

### العمليات الأساسية

#### ✅ إنشاء حادث جديد
1. اضغط على زر "➕ Nouvel Incident"
2. املأ البيانات:
   - الخطورة (حرج/متوسط/منخفض)
   - العنوان
   - بيانات المريض
3. اضغط "Créer l'incident"

#### ✅ تعيين سيارة إسعاف
1. في قائمة الحوادث، اضغط "Assigner"
2. اختر سيارة متاحة من القائمة
3. يتم التعيين تلقائياً

#### ✅ تغيير حالة السيارة
1. في جدول السيارات، اضغط على أيقونة القلم ✏️
2. اختر الحالة الجديدة:
   - `available` - متاحة
   - `busy` - مشغولة
   - `maintenance` - صيانة

#### ✅ التنقل بين الصفحات
- **Dashboard**: الصفحة الرئيسية بالمؤشرات
- **Incidents**: صفحة إدارة الحوادث بالتفصيل
- **Carte**: (قريباً) خريطة تفاعلية
- **Flotte**: (قريباً) إدارة الأسطول

---

## 🧩 المكونات (Components) {#المكونات}

### المكونات الأساسية

#### 🏠 `Sidebar.jsx` - القائمة الجانبية
**الوظيفة**: قائمة التنقل الرئيسية

**الخصائص (Props)**:
- `onNavigate`: دالة للتنقل بين الصفحات
- `currentPage`: الصفحة الحالية النشطة

**الاستخدام**:
```jsx
<Sidebar 
  onNavigate={setCurrentPage} 
  currentPage={currentPage} 
/>
```

---

#### 📊 `KPIGrid.jsx` - شبكة المؤشرات
**الوظيفة**: عرض 4 بطاقات إحصائية رئيسية

**الخصائص**:
- `kpis`: كائن يحتوي على الإحصائيات

**البيانات المعروضة**:
1. عدد السيارات المتاحة
2. الحوادث النشطة
3. متوسط وقت الاستجابة
4. التدخلات المكتملة اليوم

---

#### 🚨 `IncidentList.jsx` - قائمة الحوادث
**الوظيفة**: عرض الحوادث قيد المعالجة

**الخصائص**:
- `incidents`: مصفوفة الحوادث
- `ambulances`: مصفوفة السيارات
- `onAssign`: دالة لتعيين السيارة

**مثال**:
```jsx
<IncidentList 
  incidents={incidents}
  ambulances={ambulances}
  onAssign={handleAssignAmbulance}
/>
```

---

#### 🚑 `AmbulanceTable.jsx` - جدول السيارات
**الوظيفة**: جدول شامل بحالة كل سيارة

**الأعمدة**:
- الاسم الإذاعي
- الحالة
- الطاقم
- الموقع
- الإجراءات

---

#### 🔔 `AlertBanner.jsx` - لافتة التنبيه
**الوظيفة**: تنبيه بصري للحوادث الحرجة

**تظهر فقط إذا**: هناك حادث بدرجة `critical` في حالة `pending`

---

#### 📝 `ActivityFeed.jsx` - تدفق الأنشطة
**الوظيفة**: سجل زمني للأحداث الأخيرة

**أنواع الأنشطة**:
- `incident_created` - حادث جديد
- `ambulance_assigned` - تعيين سيارة
- `status_changed` - تغيير حالة
- `incident_completed` - إنهاء تدخل

---

#### 🪟 `Modal.jsx` - النافذة المنبثقة
**الوظيفة**: نافذة عامة للنماذج والتفاصيل

**الخصائص**:
- `isOpen`: حالة الفتح/الإغلاق
- `onClose`: دالة الإغلاق
- `title`: عنوان النافذة
- `children`: المحتوى الداخلي

---

### الصفحات (Pages)

#### 📄 `Incidents.jsx` - صفحة الحوادث الكاملة

**الميزات**:
1. **عرض شبكي**: جميع الحوادث في بطاقات منظمة
2. **فلترة**: حسب الحالة (الكل/قيد الانتظار/جاري/مكتمل)
3. **تفاصيل**: نافذة منبثقة بكامل المعلومات
4. **إنشاء**: نموذج لإضافة حادث جديد
5. **تعيين**: اختيار سيارة من القائمة المتاحة

**الألوان**:
- 🔴 أحمر: حرج (critical)
- 🟠 برتقالي: متوسط (medium)
- 🟢 أخضر: منخفض (low)

---

## 🎨 التصميم والأنماط

### نظام الألوان
```css
--primary-color: #dc2626     /* الأحمر الرئيسي */
--success-color: #16a34a     /* الأخضر للنجاح */
--warning-color: #f59e0b     /* البرتقالي للتحذير */
--danger-color: #dc2626      /* الأحمر للخطر */
--dark-color: #1e293b        /* الرمادي الداكن */
```

### التخطيط
- **Sidebar**: عرض ثابت 260px
- **Main Content**: مرن يملأ المساحة المتبقية
- **Responsive**: يتكيف مع الشاشات الصغيرة

---

## 🔧 التخصيص والتطوير

### إضافة صفحة جديدة

1. **إنشاء ملف الصفحة**:
```jsx
// src/pages/MyPage.jsx
const MyPage = () => {
  return (
    <div className="my-page">
      <h1>صفحتي الجديدة</h1>
    </div>
  );
};
export default MyPage;
```

2. **إضافة في App.jsx**:
```jsx
import MyPage from './pages/MyPage';

// في دالة render
if (currentPage === 'mypage') {
  return (
    <div className="app">
      <Sidebar onNavigate={setCurrentPage} currentPage={currentPage} />
      <main className="main-content">
        <MyPage />
      </main>
    </div>
  );
}
```

3. **إضافة في Sidebar**:
```jsx
<button 
  onClick={() => onNavigate('mypage')} 
  className={`nav-link ${currentPage === 'mypage' ? 'active' : ''}`}
>
  <i className="bi bi-star"></i>
  صفحتي
</button>
```

---

### إضافة نقطة نهاية API جديدة

1. **في db.json**:
```json
{
  "mydata": [
    { "id": 1, "name": "عنصر 1" }
  ]
}
```

2. **في api.js**:
```javascript
export const fetchMyData = async () => {
  const response = await fetch(`${API_URL}/mydata`);
  return response.json();
};
```

3. **في المكون**:
```jsx
const [myData, setMyData] = useState([]);

useEffect(() => {
  fetchMyData().then(setMyData);
}, []);
```

---

## 📊 تدفق البيانات

```
┌─────────────┐
│   Browser   │
│  (React)    │
└──────┬──────┘
       │
       │ fetch()
       ▼
┌─────────────┐
│ JSON Server │
│  (Port:5000)│
└──────┬──────┘
       │
       │ read/write
       ▼
┌─────────────┐
│   db.json   │
│  (Database) │
└─────────────┘
```

**خطوات العملية**:
1. React يرسل طلب HTTP (GET/POST/PATCH)
2. JSON Server يستقبل ويعالج الطلب
3. يقرأ/يكتب في db.json
4. يرجع البيانات بصيغة JSON
5. React يحدث الواجهة

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الصفحة لا تُحمّل
**السبب**: JSON Server غير مشغّل
**الحل**:
```bash
npm run server
```

### المشكلة: خطأ CORS
**السبب**: إعدادات الأمان في المتصفح
**الحل**: JSON Server يدعم CORS افتراضياً

### المشكلة: البيانات لا تتحدث
**السبب**: الـ State لم يُحدّث
**الحل**: تأكد من استدعاء `loadData()` بعد أي تعديل

---

## 📚 مصادر التعلم

### React
- [الموقع الرسمي](https://react.dev)
- [useState Hook](https://react.dev/reference/react/useState)
- [useEffect Hook](https://react.dev/reference/react/useEffect)

### Fetch API
- [MDN - Fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API)

### JSON Server
- [الموقع الرسمي](https://github.com/typicode/json-server)

---

## 📞 الدعم

لأي استفسار أو مشكلة:
1. راجع هذا الملف أولاً
2. افحص console.log في المتصفح
3. تحقق من تشغيل JSON Server

---

**آخر تحديث**: 14 ديسمبر 2025
**الإصدار**: 1.0.0
